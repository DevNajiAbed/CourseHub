<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CourseHub" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary Colors -->
        <item name="colorPrimary">@color/blue</item>
        <item name="colorOnPrimary">@color/dark_gray</item>

        <!-- Secondary Colors -->
        <item name="colorSecondary">@color/orange</item>
        <item name="colorOnSecondary">@color/dark_gray</item>

        <!-- Backgrounds -->
        <item name="android:windowBackground">@color/dark_gray</item>
        <item name="colorOnBackground">@color/white</item>

        <!-- Surfaces -->
        <item name="colorSurface">@color/blue_gray</item>
        <item name="colorOnSurface">@color/white</item>

        <!-- Error Colors -->
        <item name="colorError">@color/red</item>
        <item name="colorOnError">@color/white</item>
    </style>
</resources>